# Atom Checkbox card Component

## Usage

```html
<atom-checkbox-card
  title="string"
  subtitle="string"
  checked="boolean"
  (itemSelected)="onValueChange()"
  (valueChanged)="onValueChange()"
></atom-checkbox-card>
```
